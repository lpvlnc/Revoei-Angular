import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lpvlnc.revoei',
  appName: 'revoei',
  webDir: 'dist/revoei',
  bundledWebRuntime: false
};

export default config;
